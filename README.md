# Funda — Property Listings

A Nuxt 3 application for browsing Funda real-estate listings, built as a frontend engineering assignment.

## Tech Stack

- **Nuxt 3** — SSR-first framework for Vue
- **Tailwind CSS** — Utility-first styling with Funda branding
- **Zod** — Runtime validation of API responses
- **Vitest** — Unit and component testing with `@nuxt/test-utils`
- **Leaflet** — Lightweight maps (client-side only)

## Getting Started

```bash
# Install dependencies
npm install

# Copy env file and add your API key
cp .env.example .env

# Start development server
npm run dev
```

The app runs at `http://localhost:3000`.

## Testing

```bash
# Run all tests
npm test

# Run unit tests only
npm run test:unit

# Run Nuxt environment tests only
npm run test:nuxt

# Watch mode
npm run test:watch
```

## Build & Preview

```bash
npm run build
npm run preview
```

## Architecture Decisions

_To be expanded as the project progresses._

## Further Improvements

_To be documented after the MVP is complete._
